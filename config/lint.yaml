---
# Runtime configuration for OpenAPI linting.
paths:
  normalized: "docs/specifications/api"
  reports: "reports"

linting:
  fail_on_error: true
  fail_on_warning: false
  skip_on_lint_failure: false
  max_errors_per_file: 100

spectral:
  format: "json"
  verbose: false
